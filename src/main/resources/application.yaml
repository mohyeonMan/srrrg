server:
  shutdown: graceful

spring:
  application:
    name: srrrg
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/srrrg}
    username: ${SPRING_DATASOURCE_USERNAME:srrrg}
    password: ${SPRING_DATASOURCE_PASSWORD:srrrg-local-password}
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

srrrg:
  base-url: ${SRRRG_BASE_URL:https://srrrg.link}
  trust-forwarded-headers: ${SRRRG_TRUST_FORWARDED_HEADERS:false}
  safe-browsing:
    api-key: ${GOOGLE_SAFE_BROWSING_API_KEY:AIzaSyC_KJCzOLDiNowtKUm8hMg75QxJ82kXIBc}
    # api-key: ${GOOGLE_SAFE_BROWSING_API_KEY:}
    endpoint: ${GOOGLE_SAFE_BROWSING_ENDPOINT:https://safebrowsing.googleapis.com/v4/threatMatches:find}
    connect-timeout: ${GOOGLE_SAFE_BROWSING_CONNECT_TIMEOUT:2s}
    read-timeout: ${GOOGLE_SAFE_BROWSING_READ_TIMEOUT:3s}
  redirect:
    # 최근 검사 결과를 재사용하는 최대 시간임.
    verification-ttl: ${SRRRG_REDIRECT_VERIFICATION_TTL:1h}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,prometheus

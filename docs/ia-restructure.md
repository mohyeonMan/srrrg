# 정보구조 재편 기록

프로젝트·캠페인·링크 세 레벨의 탭 문법을 통일하는 작업의 진행 기록입니다.
판단 근거는 `docs/desktop-audit.md`(데스크톱 실측)와 아래 측정값입니다.

## 목표 구조

| 레벨 | 탭 | 생성 |
| --- | --- | --- |
| 프로젝트 | `개요` / `멤버` / `UTM` / `설정` | 헤딩 CTA → 대화상자 |
| 캠페인 | `개요` / `링크` / `만들기` / `설정` | 탭 유지(대량 생성 작업공간) |
| 링크 | `통계` / `링크 설정` | — (변경 없음) |

캠페인의 생성 탭을 유지하는 이유: 동적 UTM 입력 + 이동 주소 미리보기 + CSV 대량 생성
(최대 10,000행·10MB, 오류 CSV 다운로드)을 담은 **머무는 작업공간**이라 대화상자에 맞지 않습니다.
프로젝트 레벨의 두 생성은 필드 3~4개로 끝나는 단발 작업입니다.

원칙: **탭 = 머무는 작업공간, 대화상자 = 한 번에 끝나는 생성.**

---

## Phase 0 — 선행 조건 (완료)

IA 는 바꾸지 않고, 재편이 기능 후퇴 없이 가능하도록 바닥을 깔았습니다.

### 0.1 모바일 탭 라벨 실측 (320px, `/manage` 의 실제 `.console-tabs` 기준)

폰트 `600 14px`, 스트립 좌우 패딩 24px, 셸 300px.

| 탭 수 | 탭 1개당 예산 |
| --- | --- |
| 2 | 134px |
| 3 | 87px |
| **4** | **63px** |
| 6 | 39px |

| 라벨 | 실측 폭 | | 라벨 | 실측 폭 |
| --- | --- | --- | --- | --- |
| 개요 · 멤버 · 설정 · 링크 · 통계 | 28px | | 링크 목록 · 링크 생성 · 멤버 관리 · 링크 설정 | 61px |
| UTM | 32px | | UTM 관리 | 65px |
| 만들기 | 42px | | 캠페인 생성 | 75px |

판정:

- **목표 구조(4탭, 짧은 이름)**: 최대 32px vs 예산 63px → 여유 31px. 성립.
- **`~관리` 접미사**: `UTM 관리` 65px > 63px → 줄바꿈. **짧은 이름이 선택이 아니라 필수.**
- **생성을 탭에 넣는 안(6탭)**: `캠페인 생성` 75px vs 예산 39px → 약 2배 초과. **기각.**
  갭을 8px 로 줄인 뒤에도 성립하지 않으므로 이 기각은 견고합니다.
- **현재 캠페인 4탭**: `링크 목록` 61px. 갭이 24px 이던 동안 예산이 51px 이었으므로
  **320px 에서 실제로 줄바꿈하고 있었습니다.** 0.2 로 예산이 63px 이 되어 해소됐고,
  여유가 2px 뿐이라 Phase 3 의 라벨 축약(`링크` 28px, `만들기` 42px)이 실질적 의미를 갖습니다.

### 0.2 좁은 폭 탭 갭 축소 (24px → 8px)

`.console-tab { flex: 1 }` 로 탭이 균등 분할되는 폭에서 24px 갭은 과했습니다.
320px 4탭이면 스트립 276px 중 **갭만 72px(26%)** 를 먹어 라벨 예산이 51px 로 눌렸습니다.
`--space-2`(8px)로 줄여 63px 확보. 위 판정대로 **기존 줄바꿈 결함이 함께 해소**됐습니다.

### 0.3 탭 컨트롤러 공용화 + 주소화

`srrrg-campaigns.js` 와 `srrrg-projects.js` 가 같은 코드를 각자 갖고 있었습니다 —
`{prefix}-tab-{name}` / `{prefix}-panel-{name}`, roving tabindex, 좌우 순환 방향키까지 동일.
`SrrrgCommon.tabs({ list, prefix, param, initial })` 하나로 합쳤습니다.

추가된 것:

- `?tab=<name>` 동기화. `location.hash` 는 `ProjectsPageAssetTest` 가 금지하므로 쿼리 파라미터를 씁니다.
- 목록에 없는 `initial` → 첫 탭 폴백.
- 버튼·패널이 없는 이름은 예외 없이 건너뜀(`srrrg-projects.js` 가 의존했던 관용성).

**URL 은 사용자가 탭을 누를 때만 씁니다.** 초기 bind 에서는 쓰지 않습니다 —
아무것도 바뀌지 않은 시점에 주소를 더럽히지 않고, 두 묶음이 동시에 초기화되는 경우의
덮어쓰기 경합도 원천적으로 없앱니다. **`replaceState`** 를 씁니다: 탭 전환은
히스토리에 쌓일 사건이 아니고, 뒤로가기가 탭을 되짚는 대신 이전 화면으로 나가는 편이 예측 가능합니다.

`srrrg-management.js`(링크 레벨)의 탭은 **합치지 않았습니다.** ID 규칙이 다르고
(`analytics-tab`), 비활성 탭을 건너뛰며, 저장 안 된 변경이 있으면 확인 대화상자를 띄우는
`requestTab` 게이트가 있습니다. 게다가 그 화면은 secret key 게이트 뒤라 주소를 공유해도
열리지 않으므로 URL 동기화의 이득이 없습니다. 억지로 합치면 옵션만 늘고 얻는 게 없습니다.

두 묶음이 섞일 걱정은 템플릿 차원에서 이미 없습니다. `projects.html` 의 `th:if` 가
프로젝트 탭 스트립을 `campaignId == null and linkCode == null and view ∉ {utm-templates, project-settings}`
일 때만 렌더하므로, 프로젝트 스트립이 있을 때는 캠페인 스트립이 없습니다.
초기값 판정에 별도 가드를 두었다가 이 사실을 확인하고 **죽은 코드라서 제거**했습니다.

### 0.4 패널 지연 초기화 — Phase 2 로 미룸

원래 Phase 0 에 넣었다가 뺐습니다. Phase 0 은 어떤 패널이 렌더되는지 바꾸지 않으므로
지연 초기화가 필요한 시점이 아닙니다. 지금 만들면 쓰이지 않는 코드입니다.

**Phase 2 에서 반드시 필요해집니다:** 설정·UTM 조각을 `th:if` 없이 상시 렌더 탭 패널로
바꾸는 순간, `projects.html` 이 이미 8개 스크립트를 무조건 로드하고 있으므로
`srrrg-project-settings.js` 와 `srrrg-project-utm-templates.js` 의 `if (!app) return` 가드가
풀려 **프로젝트 페이지 로드마다 API 2건이 추가로 발생**합니다. 그중 설정 쪽 `loadProject()` 는
`srrrg-projects.js` 가 이미 가진 데이터를 중복 조회합니다.
기존 선례(`window.SrrrgProjectMembers?.reload(...)`)를 그대로 확장하면 됩니다.

### 검증

- 전체 테스트 226건 통과(실패 0, 오류 0, 건너뜀 34)
- 컨트롤러 기능 검사 11건 통과: 초기 페인트(`aria-selected`/`tabIndex`/`hidden`),
  초기 bind 의 URL 미기록, 클릭 시 URL 기록, 좌우 방향키 순환(양방향),
  `initial` 존중, 알 수 없는 값 폴백, 패널 누락 관용, `replaceState`(히스토리 미증가)
- 탭·패널 ID 짝 1:1 확인(캠페인 4쌍 / 프로젝트 2쌍), 중복 ID 없음
- 공개 화면 콘솔 오류 0건
- 순 변경량 +78/−58 (중복 구현 2개 → 공용 1개, 그러면서 URL 동기화와 관용성이 추가됨)

### 남은 확인 (Phase 0)

Chrome 확장이 이번 라운드 중간에 끊겨 **로그인 상태의 프로젝트·캠페인 화면에서
탭을 실제로 눌러보는 검증은 하지 못했습니다.** 컨트롤러 로직은 격리 검사 11건과
전체 테스트로 확인했고, 서버가 내보내는 HTML 의 ID 짝도 확인했습니다.
프로젝트 화면에서 아래만 눌러봐 주시면 Phase 0 은 마감됩니다.

1. 프로젝트 개요에서 `통계` ↔ `멤버` 전환 → 주소에 `?tab=members` 가 붙는지
2. 그 주소를 새로 열었을 때 `멤버` 탭으로 복원되는지
3. 캠페인에서 `개요`/`링크 목록`/`링크 생성`/`설정` 전환과 좌우 방향키
4. 캠페인 링크가 0개일 때 "만들기로 이동" 버튼이 `링크 생성` 탭으로 넘기는지
5. 320px 에서 캠페인 4탭 라벨이 한 줄로 보이는지

사용자 육안 확인 완료(정상). Phase 0 마감.

---

## Phase 1 — 레일 종 분리와 생성 대화상자 (완료)

탭 구조는 건드리지 않고, 생성이 화면을 파괴하는 문제와 레일의 종 혼합을 해결했습니다.

### 1.1 생성을 헤딩 CTA + 대화상자로

이전에는 레일의 `단축링크 생성` / `캠페인 생성` 을 누르면 `view=create-link|create-campaign` 상태가 되고
`showProjectPanel()` 이 요약(`#project-home-panel`)과 탭 바(`#project-view-tabs`)를 함께 숨겼습니다.
**무언가를 만들려고 누르면 보고 있던 화면과 프로젝트의 지도가 사라졌습니다.**

`#create-link-dialog` / `#create-campaign-dialog` 두 대화상자로 옮겼습니다.
새 컴포넌트를 만들지 않고 `#create-project-dialog` 가 이미 쓰던 `<dialog>` + `.project-create-dialog` 패턴을 그대로 씁니다.
폼의 모든 ID 를 유지해 검증·만료 옵션·결과·복사 동작은 손대지 않았습니다.

**계획에서 바꾼 것 — 레일의 생성 항목은 남기지 않고 삭제했습니다.**
계획에는 "레일 shortcut 으로 남긴다" 고 적었는데, `.projects-heading` 이 `.projects-layout`
**바깥**이라 캠페인·링크·설정 어느 화면에서도 보인다는 것을 확인했습니다.
헤딩 CTA 가 이미 어디서든 1클릭이므로 레일에 같은 것을 둘 이유가 없습니다.
(shortcut 이 평탄함을 지킨다는 근거는 탭으로 들어갈 `UTM`·`설정` 에만 적용됩니다.)

`＋ 만들기 ▾` 메뉴 대신 **버튼 두 개**로 갔습니다. 열고닫기 상태·외부 클릭 처리·메뉴 키보드 의미가
전부 불필요해지고, 좁은 폭 문제는 `flex-wrap` 으로 해결됩니다.

### 1.2 레일의 종(種) 분리

레일의 네 항목은 모두 `.project-activity-item` 이었습니다 — **아래 콘텐츠 목록의 캠페인 항목과 같은 클래스**로,
동작(만들기)·섹션(UTM·설정)·객체(캠페인)가 한 열에서 구분되지 않았습니다.
생성 둘은 헤딩으로 옮기고, 남은 둘은 `.project-rail-shortcut` 으로 분리해 26px 글리프 배지를 버렸습니다.

### 1.3 함께 정리한 죽은 코드

- `showProjectPanel()` 전체 — 세 상태를 오갈 일이 없어졌고, 남은 것은 템플릿 기본 상태와 탭 컨트롤러 소관
- `state.canEdit` 대입 — 읽는 곳이 `showProjectPanel()` 뿐이었음
- `.project-activity-kind.template` / `.settings` — 활동 목록은 `campaign`·`link` 만 내보냄
- `.project-link-create` (브랜드색 상단 테두리) — 그 판이 사라짐
- `focusOpenedPanel()` 의 대화상자 제목 두 개 — 대화상자는 항상 DOM 에 있어 그대로 두면
  알 수 없는 `view` 값에서 숨은 제목으로 포커스가 갔음. 포커스는 `<dialog>` 가 관리

### 1.4 작업 중 발견해 고친 결함 2건

**결함 A — 대화상자를 `#project-detail` 안에 두면 렌더되지 않는다.**
처음에 판이 있던 자리에 그대로 대화상자를 넣었는데, `#project-detail` 은 캠페인·링크·설정 화면에서
`hidden`(= `display: none`)이 됩니다. `display: none` 조상 아래의 `<dialog>` 는 `showModal()` 해도
렌더되지 않습니다. 페이지 수준(`#confirm-dialog` 옆)으로 옮겼고,
**테스트에 문서 순서 단정을 넣어** 다시 안으로 들어가면 실패하게 했습니다.

**결함 B — 420px 대화상자에서 만료 옵션 라벨이 줄바꿈된다.**
`.expire-options` 기본값은 `repeat(6, minmax(0, 1fr))` 이고, 열 수를 줄이는 미디어쿼리는
**뷰포트**를 봅니다 — 데스크톱 뷰포트에서 이 폼이 420px 대화상자 안에 있다는 걸 알 수 없습니다.

| | 열 | 버튼 폭 | `직접 입력` |
| --- | --- | --- | --- |
| 옮긴 직후 | 6열 | 56px | **2줄** |
| `auto-fit` 적용 | 4열 2행 | 89px | 1줄 |

`#create-link-dialog .expire-options` 로 **이 대화상자에만** `repeat(auto-fit, minmax(76px, 1fr))` 을 걸었습니다.
공용 컴포넌트 기본값과 홈의 생성 도구는 건드리지 않았습니다(홈 실측: 여전히 6열 106px, 전부 1줄).

`scrollWidth` 로는 이 결함이 잡히지 않습니다 — 줄바꿈은 넘침이 아니기 때문입니다.
`Range.getClientRects().length` 로 실제 줄 수를 세어 확인했습니다(320px 헤더 로그인 버튼과 같은 함정).

### 1.5 헤딩 CTA 의 좁은 폭 동작

`.projects-heading` 에 `flex-wrap: wrap` + `gap` 을 주어 CTA 가 제목을 눌러 줄이지 않고 아래 줄로 갑니다.
다만 320px 에서 두 버튼이 필요 폭보다 **1px 모자라** 우연히 2줄로 쌓였습니다 — 라벨이 조금만 바뀌어도
한 줄/두 줄이 뒤집히는 상태였습니다. `@media (max-width: 520px)` 에서 컨테이너를 전폭으로 두고
버튼에 `flex: 1` 을 주어 **의도된 한 줄**(각 146px)로 고정했습니다.

### 검증

- 전체 테스트 226건 통과(실패 0). 새 단정: 대화상자 ID·CTA·shortcut 클래스,
  인라인 판 부재, **대화상자가 `#project-detail` 보다 앞에 있음**
- 서버 렌더 HTML 확인: `create-link-dialog`(4684) / `create-campaign-dialog`(7700) 모두
  `project-detail`(10855) 앞. 구 ID 4개 모두 제거됨
- 태그 균형(dialog 4/4, section 9/9, form 3/3 등), CSS 중괄호 균형
- 만료 옵션 before/after 실측(위 표), 홈 미영향 실측
- 헤딩 CTA 1280px 1행 / 320px 전폭 1행 146px, 가로 넘침 없음
- 콘솔 오류 0건

### 남은 확인 (Phase 1, 로그인 필요)

Chrome 확장이 계속 연결되지 않아 로그인 상태의 실제 클릭은 확인하지 못했습니다.

1. 헤딩의 `링크 만들기` → 대화상자가 열리고 원본 URL 칸에 포커스가 가는지
2. 링크 생성 성공 → 대화상자 안에 단축 URL + 복사 버튼이 나오고, 닫으면 **보고 있던 탭이 그대로**인지
3. 두 번째로 열었을 때 지난 결과·메시지가 지워져 있는지
4. `캠페인 만들기` → 성공 후 만든 캠페인 화면으로 이동하는지
5. **캠페인 화면에서도** 헤딩 CTA 가 보이고 대화상자가 정상 렌더되는지(결함 A 회귀 확인)
6. 조회 전용(VIEWER) 계정에서 CTA 와 `프로젝트 설정` shortcut 이 숨는지
7. 레일에서 캠페인 항목과 `UTM 템플릿`·`프로젝트 설정` 이 서로 다른 모양으로 보이는지

---

## Phase 1 검수 라운드 — 로그인 실화면 + 7폭 회귀 (완료)

Chrome 확장이 복구되어 **로그인 상태의 실제 화면**에서 Phase 1 의 미검증 항목을 모두 확인했습니다.
폭 순회는 서버가 렌더한 마크업을 그대로 가져와(`fetch` + `DOMParser`) 실제 CSS 위에 올려 측정했습니다 —
페이지 HTML 은 `permitAll` 이라 인증 없이 받을 수 있고 JS 를 실행하지 않으므로 401 리다이렉트도 없습니다.

### 실화면 동작 검증 (Chrome, 로그인 상태)

프로젝트 개요에서 10건, 캠페인 화면에서 6건 전부 통과.

- 링크 대화상자: 열림 · `:modal` · 원본 URL 칸 포커스 · 결과 영역 초기 숨김 · 닫힘
- **닫은 뒤 요약과 탭 바가 그대로 남음** — Phase 1 의 목적이 실제로 달성됨
- 재오픈 시 지난 결과·메시지 지워짐
- 캠페인 대화상자: 열림 · 이름 칸 포커스
- **결함 A 회귀 확인:** 캠페인 화면(`#project-detail` 이 `hidden`)에서 대화상자가 **420×461 로 실제 렌더**됨
- 헤딩 CTA 가 캠페인 화면에서도 보임

부수 확인: `?tab=link-create` 로 진입하니 캠페인의 `링크 생성` 탭이 `aria-selected="true"` 로 복원됐습니다.
Phase 0 의 주소화가 로그인 실화면에서 종단 동작함을 확인한 것입니다.

### 이번 라운드에 고친 결함 1건 — 레일 shortcut 정렬축

캡처를 보고 shortcut 이 섹션 라벨보다 안쪽으로 들어가 보여 측정했습니다.

| 레일 요소 | 텍스트 x (수정 전) | 수정 후 |
| --- | --- | --- |
| 섹션 라벨 "프로젝트 관리" | 17 | 17 |
| **`.project-rail-shortcut`** | **25** | **17** |
| 섹션 라벨 "콘텐츠" | 17 | 17 |
| 콘텐츠 항목 글리프 배지 | 17 | 17 |

원인: 부모 `.project-rail-actions` 가 이미 `--space-2`(8px)를 갖는데 제가 shortcut 에
`--space-4`(16px)를 줘서 24px + 테두리 1 = 25 가 됐습니다. shortcut 을 `--space-2` 로 바꿔
**레일 안 네 종류가 한 축(데스크톱 17 / 드로어 33)에** 섭니다. 높이 44px 은 유지됩니다.

측정 중 드로어에서 41 이 나와 새 결함으로 의심했는데, **인앱 브라우저의 CSS 캐시**였습니다.
서브된 CSS 를 CSSOM 으로 확인(`padding: 0 var(--space-2)`)하고 하드 리로드하니 33 으로 일치했습니다.
계산값(17+8+8=33)과 어긋나면 캐시를 먼저 의심할 것.

### 문구 수정 2건

**① 제출 버튼의 명사 불일치.** CTA `링크 만들기` → 제목 `링크 만들기` → 제출 `단축 URL 만들기` 로
한 흐름에서 같은 대상을 두 이름으로 불렀습니다. 제출을 `링크 만들기` 로 맞췄습니다
(캠페인 대화상자가 이미 제목=제출인 것과 같은 형태). 홈의 익명 단축기는 자기 맥락이 있어 그대로 뒀습니다.

**② 무엇이 만들어지는지 밝힘.** 프로젝트 레벨과 캠페인 레벨의 "링크 만들기" 가 서로 다른 것을
만드는데(단일링크 vs 캠페인 링크) 문구가 그 차이를 말하지 않았습니다 — IA 검토에서 지적한 모호함입니다.

- 전: `이 프로젝트의 전용 도메인으로 단축 URL을 만듭니다.`
- 후: `캠페인에 속하지 않는 단일링크를 이 프로젝트 도메인으로 만듭니다.`

`단일링크` 는 레일 필터와 프로젝트 요약이 이미 쓰는 표현이라 새 용어를 만들지 않았습니다.

### 7폭 회귀 검수 (신선한 CSS, 실제 마크업)

| 폭 | 레일 단일축 | CTA 행 | CTA 버튼폭 | 링크 대화상자 | 캠페인 대화상자 | UI 줄바꿈 | 넘침 | 가로넘침 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1920 | ✓ 17 | 1 | 109 / 123 | 420×481 | 420×427 | 없음 | 0 | 없음 |
| 1440 | ✓ 17 | 1 | 109 / 123 | 420×481 | 420×427 | 없음 | 0 | 없음 |
| 1280 | ✓ 17 | 1 | 109 / 123 | 420×481 | 420×427 | 없음 | 0 | 없음 |
| 430 | ✓ 33 | 1 | 201 / 201 | 394×513 | 394×447 | 없음 | 0 | 없음 |
| 390 | ✓ 33 | 1 | 181 / 181 | 354×513 | 354×447 | 없음 | 0 | 없음 |
| 360 | ✓ 33 | 1 | 166 / 166 | 324×513 | 324×447 | 없음 | 0 | 없음 |
| 320 | ✓ 33 | 1 | 146 / 146 | 284×561 | 284×447 | 없음 | 0 | 없음 |

- **CTA 는 전 폭에서 한 줄**입니다(320px 에서 1px 차이로 우연히 2줄이던 것을 전폭 한 줄로 고정한 결과).
- 대화상자의 라벨·입력이 전 폭에서 **단일 축(21)**. 만료 옵션은 폭에 따라 2~3행으로 접히며 라벨 줄바꿈 없음.
- 320px 에서 대화상자 폭이 284px 인 것은 브라우저 UA 의 `max-width: calc(100% - 6px - 2em)` 때문입니다
  (우리 CSS 는 300px 을 주려 함). 안전 여백이라 그대로 둡니다.

### 접근성 (320px 두 대화상자)

- 라벨 미연결 입력 **0건** (모두 `label[for]` 또는 감싸는 `label`)
- 24px 미만 타깃 **0건**
- `aria-labelledby` 연결 정상, 닫기 버튼 `aria-label="닫기"`
- 넘침 0, 키보드 포커스는 네이티브 `<dialog>` 가 트랩 관리
- 캠페인 `링크 생성` 탭의 `현재 캠페인용 CSV 양식 다운로드`(18px)는 최단 이웃이 **408px** 이라
  WCAG 2.5.8 간격 예외를 통과합니다(기존 요소, 이번 변경과 무관).

### 수용한 한계 1건 — 320×568 에서 대화상자 하단 잘림

가용 높이가 아주 낮은 기기(iPhone SE 급)에서 링크 대화상자가 **284×532**, UA `max-height: calc(100% - 36px)`
에 걸리고 `overflow-y: auto` 로 **29px 만큼 스크롤**됩니다. 제출 버튼이 아래로 조금 잘립니다.

고치지 않은 이유: 스크롤로 도달 가능하고, 잘림이 29px 이며, 해결하려면 이 코드베이스에 없는
**높이 기준 브레이크포인트**(`@media (max-height: …)`)를 새로 도입해야 합니다.
반응형 규칙의 축을 하나 늘리는 비용이 이득보다 큽니다. 320×800 이상에서는 전부 화면 안에 들어옵니다.

### 검증 요약

- 전체 테스트 **226건 통과**(실패 0, 오류 0, 건너뜀 34)
- 실화면 동작 16건 통과, 7폭 회귀 전부 통과
- 콘솔 오류 **0건**(로그인 상태 프로젝트·캠페인 화면, 공개 화면)
- 캡처로 시각 확인: 레일 종 구분(배지 없는 shortcut vs C 배지 콘텐츠 항목),
  레일 단일 정렬축, 320px 대화상자 전체, 캠페인 화면 위 대화상자 렌더

---

## Phase 2 — 프로젝트 4탭 재편 (완료)

프로젝트가 캠페인·링크 레벨과 같은 문법을 갖도록, 별도 화면이던 UTM·설정을 탭으로 들였습니다.

### 2.1 탭 구성

`개요` / `멤버` / `UTM` / `설정`. `통계` 탭은 없애고 개요가 통계를 품습니다 —
기본 탭이 이미 통계였으므로 들어왔을 때 보이는 화면은 그대로입니다.

**라벨을 "멤버 관리"·"UTM 관리" 로 하지 않은 이유:** 320px 에서 탭 하나에 63px 이 돌아가는데
`UTM 관리` 는 65px 이라 줄바꿈됩니다(0.1 실측). 모든 탭이 관리이므로 접미사는 정보를 더하지 않고,
맥락은 각 패널의 카드 제목이 말합니다. 실측 결과 `개요`·`멤버`·`설정` 28px, `UTM` 32px 로
63px 예산에 여유 31px 입니다.

### 2.2 한 축(`?tab=`)으로 통일

`view=utm-templates` / `view=project-settings` 로 `#project-detail` 을 통째로 대체하던 두 화면을
탭 패널로 바꿨습니다. 이제 프로젝트 레벨의 상태는 `?tab=` 하나로 표현됩니다.
예전 주소로 저장된 링크·북마크는 `LEGACY_VIEW_TABS` 가 해당 탭으로 넘깁니다(실화면 확인).

### 2.3 레일 shortcut 과 탭의 일치

shortcut 은 `?projectId=N&tab=utm|settings` 를 가리키고, 활성 표시는 `paintRailActive()` 가
탭이 바뀔 때마다 다시 칠합니다. 실화면 확인: UTM 탭에서 UTM shortcut 만 활성,
설정 탭에서 설정 shortcut 만 활성, 개요·멤버 탭에서는 둘 다 비활성.

`SrrrgCommon.tabs` 의 훅은 `onActivate`(처음 한 번)가 아니라 `onSwitch`(매번)로 만들었습니다.
"처음 한 번" 이 필요한 쪽(패널 지연 조회)과 "매번" 이 필요한 쪽(shortcut 활성 표시)이 함께 있어서,
한 번만 부르는 훅으로 만들면 후자가 깨집니다. 매번 부르고 호출자가 걸러 쓰는 편이 일반적입니다.

### 2.4 패널 지연 초기화 (Phase 0 에서 미뤄 둔 0.4)

조각이 상시 렌더되므로 그대로 두면 페이지를 열 때마다 모든 패널이 API 를 부릅니다.
`window.SrrrgProjectSettings` / `SrrrgProjectUtmTemplates` 를 새로 노출하고
자체 초기 조회를 없앴습니다. `SrrrgProjectMembers` 는 자체 조회와 `srrrg-projects.js` 의
호출이 겹쳐 같은 목록을 두 번 부르기도 했는데, 그 중복도 함께 정리했습니다.

| | 이전 | 이후 |
| --- | --- | --- |
| 프로젝트 페이지 로드 시 API | 7건 | **5건** |
| `/members`·`/invitations`·`/projects/{id}` | 항상 | 멤버·설정 탭을 열 때만 |

개요(통계)는 `srrrg-statistics.js` 가 스스로 초기 조회를 하므로 지연 대상에서 뺐습니다 —
기본 탭이라 미룰 이득이 없고, 부르면 가장 비싼 통계를 두 번 조회합니다.

### 2.5 작업 중 발견해 고친 결함 3건

**결함 C — 초기 탭이 개요가 아니면 패널이 영구히 빈 상태로 남았다.**
`?tab=settings` 로 들어오면 설정 패널의 이름 칸이 비어 있고 `/api/web/projects/1` 이
아예 호출되지 않았습니다. 원인은 **defer 스크립트 실행 순서와 API 응답의 경쟁**입니다.
`srrrg-projects.js` 가 패널 조각들보다 앞에 실려, localhost 에서 `/api/web/projects` 응답이
뒤쪽 스크립트 다운로드보다 먼저 끝나면 `window.SrrrgProjectX` 가 아직 없는 상태로
첫 탭의 지연 조회가 일어나 `?.` 가 조용히 넘어갔습니다. 게다가 이미 `loadedTabs` 에
표시돼 다시 시도할 길도 없었습니다.

진단은 `performance.getEntriesByType('resource')` 로 했습니다 — 내 스크립트가 실행되기 전의
요청까지 남아 있어 "호출이 아예 없었다" 를 확정할 수 있습니다.
`loadedTabs` 에 이미 있었는지는 다른 탭을 거쳐 되돌아왔을 때 재조회가 일어나는지로 갈랐습니다.

고친 방법 두 가지(둘 다 적용):
- `projects.html` 에서 **`srrrg-projects.js` 를 맨 뒤로** 옮겨 순서 의존을 없앰(결정적 해결)
- `loadTabOnce` 가 **조각이 없으면 "불러왔다" 고 표시하지 않도록** 함(표시해 버리면 재시도 불가)

**결함 D — 주소에 `projectId` 가 없으면 설정·UTM 탭이 통째로 죽는다.**
두 조각은 `projectId` 가 없으면 IIFE 를 `return` 으로 끝냈습니다. 별도 화면이던 시절에는
`?projectId=N&view=...` 로만 도달해 문제가 없었지만, 탭이 된 뒤로는 `/projects`(자동 선택)에서도
열립니다. 그 상태에서는 `reload` 조차 노출되지 않아 탭이 빈 채로 남습니다.
`let projectId` 로 바꾸고 가드를 로드 함수 안으로 옮겨, `reload(id)` 가 프로젝트를 알려 주게 했습니다.
실화면 확인: `/projects` 에서 설정 탭이 "내 프로젝트" 를 정상 표시.

**결함 E — 문구가 같은 말을 두세 번 반복했다.**
탭 패널이 되면서 페이지 제목·탭 라벨과 겹치는 문구가 드러났습니다.

| 위치 | 이전 | 이후 |
| --- | --- | --- |
| 설정 패널 h2 | `내 프로젝트 · 설정` (JS 주입) | `설정` (sr-only, 정적) |
| 멤버 패널 h2 | `내 프로젝트 · 멤버 및 초대` (JS 주입) | `멤버 및 초대` (sr-only, 정적) |
| 카드 "프로젝트 이름" 의 필드 라벨 | `프로젝트 이름` (중복) | sr-only |
| 카드 "서브도메인" 의 필드 라벨 | `서브도메인` (중복) | sr-only |

제목 위계(h1 > h2)와 입력의 접근 가능한 이름은 스크린리더에 필요하므로 지우지 않고 `sr-only` 로 남겼습니다.
설정 패널의 카드 제목↔라벨 중복은 **2건 → 0건**(실측).

개요의 `내 프로젝트 통계` 는 그대로 뒀습니다. 이 조각은 캠페인 화면과 공유되는데
그쪽에서는 `훈삼캠페인 통계` 가 되어 프로젝트 통계와 구분하는 정보가 됩니다.

### 검증

**실화면(Chrome, 로그인 상태)**

- 네 탭 렌더·기본 활성 개요·패널 배타 표시
- 탭 전환 시 `?tab=` 기록, 되돌아와도 **재조회 없음**(1 → 1)
- shortcut 활성 표시가 탭과 정확히 일치(UTM/설정/개요·멤버 각각 확인)
- 좌우 방향키 4탭 순환(정·역), roving tabindex `0` 하나, 포커스는 탭 버튼에 유지
- 네 탭 각각을 **초기 탭**으로 진입해 데이터 로드 확인(결함 C 회귀)
- `/projects`(자동 선택)에서 설정·UTM 탭 로드 확인(결함 D 회귀)
- 예전 `view=project-settings` 주소 → 설정 탭으로 이동
- 기본 탭에서 `/members`·`/invitations`·`/projects/{id}` **미호출**(지연 유지)

**7폭 회귀** — 320 / 360 / 390 / 430 / 1280 / 1440 / 1920

| 폭 | 탭 행 | 탭 폭 | 레일 단일축 | CTA 행 | 설정 본문 단일축 | 넘침 | 가로넘침 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 320 | 1 | 63 ×4 | ✓ 33 | 1 | ✓ 19 | 0 | 없음 |
| 360 | 1 | 73 ×4 | ✓ 33 | 1 | ✓ 19 | 0 | 없음 |
| 390 | 1 | 80 ×4 | ✓ 33 | 1 | ✓ 19 | 0 | 없음 |
| 430 | 1 | 90 ×4 | ✓ 33 | 1 | ✓ 19 | 0 | 없음 |
| 1280 | 1 | 28/28/32/28 | ✓ 17 | 1 | ✓ 25 | 0 | 없음 |
| 1440 | 1 | 28/28/32/28 | ✓ 17 | 1 | ✓ 25 | 0 | 없음 |
| 1920 | 1 | 28/28/32/28 | ✓ 17 | 1 | ✓ 25 | 0 | 없음 |

탭 라벨 줄바꿈 0건, 탭 스트립 가로 스크롤 0건. 데스크톱에서 탭 폭이 라벨 폭과 같은 것은
`flex-grow: 0` + 라벨 폭 정렬이고, 좁은 폭에서는 `flex: 1` 균등 분할입니다.

- 전체 테스트 **226건 통과**(실패 0)
- 콘솔 오류 **0건**(네 탭 순회 + 대화상자 2종 열고 닫기 포함)

**측정 도구에서 걸러낸 오탐 2건(결함 아님)**

- `sr-only` 요소는 `width: 1px; overflow: hidden` 이 정의라 넘침으로 잡힌다 → 검출에서 제외
- `.danger-zone` 본문이 2px 안쪽인 것은 `border-left: 3px` 위험 마커 때문 → 테두리 폭 보정 후 판정
- 서브픽셀(`--panel-padding` 의 `2.2vw`)로 21.2/20.6 이 갈리던 것은 반올림해 판정

### 하지 않은 것

- **캠페인·링크 레벨은 손대지 않았습니다.** 캠페인 `링크 생성` 탭은 CSV 대량 생성이 있는
  작업공간이라 유지가 맞고(Phase 1 에서 판단), 링크 레벨 `통계`/`링크 설정` 은 이미 올바른 짝입니다.
  캠페인 라벨 축약(`링크 목록`→`링크`, `링크 생성`→`만들기`)은 Phase 3 항목으로 남습니다.
- UTM 탭을 초기 탭으로 열면 `/utm-templates` 가 2회 호출됩니다(캠페인 만들기 대화상자의 select 용 +
  패널용). `requestShared` 로 합칠 수 있지만 UTM 패널은 생성·삭제 후 다시 불러야 하므로
  캐시된 응답을 공유하면 오래된 데이터가 남습니다. 직접 진입할 때만 생기는 1회 중복이라 그대로 뒀습니다.

---

## Phase 3 — 캠페인 탭 라벨 축약 (완료)

`링크 목록` → `링크`, `링크 생성` → `만들기`. 프로젝트 탭에 적용한 축약 원칙을 캠페인에도 맞췄습니다.

### 근거와 정직한 범위

**지금 깨져 있는 것을 고치는 변경이 아닙니다.** Phase 0 의 갭 축소(24px → 8px)로 320px 탭 예산이
이미 63px 이 되어, 축약 전에도 줄바꿈은 없습니다(실측 확인). 이 변경이 얻는 것은 **여유**입니다.

| 320px 탭 (예산 63px) | 축약 전 글자폭 / 여유 | 축약 후 글자폭 / 여유 |
| --- | --- | --- |
| 개요 | 28 / 35 | 28 / 35 |
| 링크 목록 → **링크** | 61 / **2** | 28 / **35** |
| 링크 생성 → **만들기** | 61 / **2** | 42 / **21** |
| 설정 | 28 / 35 | 28 / 35 |

여유 2px 은 라벨이 한 글자 늘거나 폰트 폴백이 달라지는 것만으로 뒤집히는 폭입니다.
가장 빡빡한 탭의 여유가 **2px → 21px** 이 됩니다.

라벨에서 뺀 정보는 각 패널 제목이 이미 말합니다 — `캠페인 링크`(목록), `개인화 링크 만들기`(생성).
패널 제목이 탭 라벨과 겹치지 않는지 확인했고, 네 패널 모두 각자 정보를 더합니다:
`훈삼캠페인 통계` / `캠페인 링크` / `개인화 링크 만들기` / `캠페인 정보`.
(설정 패널에서 있었던 제목·라벨 중복 같은 문제는 캠페인 쪽에 없었습니다.)

### 검증

**실화면(Chrome, 로그인 상태, 캠페인 화면)**

- 라벨 `개요 / 링크 / 만들기 / 설정`
- 네 탭 전환과 `?tab=` 동기화, 좌우 방향키 4탭 순환(정상)
- 빈 상태의 "링크 만들기" 버튼 존재 확인(만들기 탭으로 보내는 경로 유지)
- 콘솔 오류 0건

**7폭 회귀** — 캠페인 탭 스트립

| 폭 | 탭 폭 | 행 | 줄바꿈 | 스트립 스크롤 | 가로넘침 |
| --- | --- | --- | --- | --- | --- |
| 320 | 63 ×4 | 1 | 없음 | 없음 | 없음 |
| 360 | 73 ×4 | 1 | 없음 | 없음 | 없음 |
| 390 | 80 ×4 | 1 | 없음 | 없음 | 없음 |
| 430 | 90 ×4 | 1 | 없음 | 없음 | 없음 |
| 1280 | 28/28/42/28 | 1 | 없음 | 없음 | 없음 |
| 1440 | 28/28/42/28 | 1 | 없음 | 없음 | 없음 |
| 1920 | 28/28/42/28 | 1 | 없음 | 없음 | 없음 |

- 전체 테스트 **226건 통과**(실패 0)

---

## 세 레벨 최종 상태

| 레벨 | 탭 | 생성 |
| --- | --- | --- |
| 프로젝트 | `개요` / `멤버` / `UTM` / `설정` | 헤딩 CTA → 대화상자 |
| 캠페인 | `개요` / `링크` / `만들기` / `설정` | 탭 유지(대량 생성 작업공간) |
| 링크 | `통계` / `링크 설정` | — |

세 레벨이 모두 `?tab=` 한 축을 쓰고, 짧은 명사 라벨을 쓰며, 설정을 탭으로 갖습니다.
생성은 무게로 갈립니다 — 필드 몇 개로 끝나는 단발은 대화상자, CSV 대량 처리가 있는 반복 작업은 탭.

레일은 객체(캠페인·단일링크) 목록 + 섹션 shortcut(UTM·설정) 두 종류만 담고,
shortcut 의 활성 표시는 탭과 항상 일치합니다.

---

## 후속 수정 — 삭제 영역 폭 불일치

프로젝트 설정에서 `프로젝트 삭제` 카드만 형제 카드보다 좁다는 지적을 받아 실측했습니다.

| 카드 | 폭 | 오른쪽 여백 |
| --- | --- | --- |
| 프로젝트 이름 | 1050 | 0 |
| 서브도메인 | 1050 | 0 |
| 기존 익명 링크 편입 | 1050 | 0 |
| **프로젝트 삭제** | **820** | **230** |

원인은 `.danger-zone` 의 `max-width: 820px` 입니다. Phase 1 에서 중복 정의를 `components.css` 로
합칠 때 `management.css` 쪽 값을 그대로 가져왔는데, 이 상한은 어디서도 맞지 않았습니다 —
`/manage` 에서도 형제인 `.settings-form` 에는 상한이 없어 삭제 영역만 좁습니다
(`max-width` 는 `management.css` 전체에서 그 한 줄뿐이었습니다).

삭제 영역은 어디에 놓이든 형제 판과 같은 폭이어야 하므로 상한을 제거했습니다.
고정 상한으로는 컨테이너가 달라질 때마다 맞출 수 없습니다.

검증(실측): 프로젝트 설정 4개 카드 전부 1050px, 캠페인 설정 5개 카드 전부 1050px, 오른쪽 여백 0.
전체 테스트 226건 통과.

## 알려진 기능 공백 — UTM 템플릿 이름 변경·삭제

화면에는 템플릿 **생성**, **필드 추가**, **필드 삭제** 만 있습니다.
템플릿 자체의 이름 변경과 삭제 버튼이 없습니다.

서버는 이미 지원합니다.

| 동작 | 엔드포인트 | 화면 |
| --- | --- | --- |
| 템플릿 생성 | `POST /projects/{id}/utm-templates` | 있음 |
| 템플릿 이름 변경 | `PATCH /projects/{id}/utm-templates/{templateId}` | **없음** |
| 템플릿 삭제 | `DELETE /projects/{id}/utm-templates/{templateId}` | **없음** |
| 필드 추가 | `POST .../fields` | 있음 |
| 필드 삭제 | `DELETE .../fields/{fieldId}` | 있음 |

`UtmTemplateService.doDelete` 는 사용 중인 캠페인이 있으면 거부하고
"이 템플릿을 사용 중인 캠페인이 있어 삭제할 수 없습니다. 먼저 캠페인의 템플릿을 변경하세요." 를 돌려줍니다.
즉 참조 무결성은 서버가 이미 지키고 있어, 화면에 붙이는 쪽은 이 오류 메시지를 그대로 보여 주면 됩니다.
